# Cynosure Bridge Configuration

# Server Configuration
PORT=3000
HOST=0.0.0.0
NODE_ENV=development

# Claude Configuration
# For Claude MAX subscription, API key is not required
# ANTHROPIC_API_KEY=your-api-key-here
CLAUDE_PATH=/Users/laptop/.npm-global/bin/claude

# Project Settings
WORKING_DIRECTORY=./
MAX_TURNS=5
TIMEOUT=60000

# Security Configuration
# Comma-separated list of allowed API keys (optional)
# PROXY_API_KEYS=key1,key2,key3

# Rate Limiting
ENABLE_RATE_LIMITING=true
RATE_LIMIT_WINDOW=60000
RATE_LIMIT_MAX=100

# CORS Configuration
# Comma-separated list of allowed origins (* for all)
CORS_ORIGINS=*

# Logging
LOG_LEVEL=info